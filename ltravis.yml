language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
env:
  - CI=true
script:
  - echo 'start exec script...'
  - npm run build-storybook
deploy:
  github_token: $coding_token
  local_dir: storybook-static
  on:
    branch: master